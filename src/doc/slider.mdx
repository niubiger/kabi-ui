---
name: Slider
route: /components/slider
menu: Components
---

import { Props, Playground } from 'docz';
import { Slider } from '../';

# Slider

## Slider Props

<Props of={Slider} />

## Slider 示例
<Playground>
    {() => {
        const [val, setVal] = React.useState(0)
        return (
            <div style={{ padding: 20, width: 200 }}>
                <Slider
                    value={val}
                    step={10}
                    onChange={val => console.log('change when mousemove', val)}
                    onChangeCommited={(val => console.log('change commited after mouseup', val))}
                />
            </div>
        );
    }}
</Playground>